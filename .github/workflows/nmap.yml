name: maigret 👁
on:  
 workflow_dispatch:
   inputs:
      target:
        description: 'Enter target username'
        required: true
jobs:
  Maigret:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name:  Setup and cmd run 
        run: | 
             pip3 install maigret 
             maigret ${{github.event.inputs.target }} -a >>output.txt
      - name: Results
        run: echo "$(<output.txt )"
